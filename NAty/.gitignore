# Archivos generados por el sistema operativo
.DS_Store
Thumbs.db

# Directorio node_modules generado por npm
node_modules/

# Archivo de registro de errores
error.log

# Archivo .env con variables de entorno sensibles
.env

# Directorio donde se almacenan las dependencias instaladas por npm
# Puedes excluir esto si prefieres mantener las dependencias en tu repositorio
# Pero es una práctica común excluirlo y luego instalar las dependencias en el servidor
# Si decides incluirlo, asegúrate de no incluir node_modules en tu repositorio
# Y excluye solo package-lock.json o yarn.lock en su lugar.
# /packages-lock.json
# /yarn.lock
